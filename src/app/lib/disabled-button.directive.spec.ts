import { DisabledButtonDirective } from './disabled-button.directive';

describe('DisabledButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new DisabledButtonDirective();
    expect(directive).toBeTruthy();
  });
});
