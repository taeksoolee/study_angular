import { RequirementPipe } from './requirement.pipe';

describe('RequirementPipe', () => {
  it('create an instance', () => {
    const pipe = new RequirementPipe();
    expect(pipe).toBeTruthy();
  });
});
