import { DisableButtonPipe } from './disable-button.pipe';

describe('DisableButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new DisableButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
